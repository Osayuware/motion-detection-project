while True:
    if input.rotation(Rotation.PITCH) > 150:
        light.set_all(color.rgb(255,0,0))
    else:
        "Sound Level" + input.sound_level()
        input.sound_level() > 150
        music.siren.play_until_done()