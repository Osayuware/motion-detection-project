while (true) {
    if (input.rotation(Rotation.Pitch) > 150) {
        light.setAll(color.rgb(255, 0, 0))
    } else {
        "Sound Level" + input.soundLevel()
        input.soundLevel() > 150
        music.siren.playUntilDone()
    }
    
}
